package com.bank;

public class SalaryAccount extends AbstractAccount {
    public SalaryAccount(String id, String ownerId, double initialBalance) {
        super(id, ownerId, initialBalance);
    }
}
